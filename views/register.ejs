    <%- include("partials/header.ejs") %>
    <form class="register-form" action="/register" method="POST">
        <% if (locals.registered) { %>
            <h1>Congratulations! You have been successfully registered.</h1>
            <a class="go-back-home" href="/">Go back to Home Page</a>
        <% } else { %>
            <h1>Register</h1>
            <label for="email">
                <span>Email Id</span>
                <input id="email" type="email" name="email" placeholder="example@gmail.com" />
            </label>
            <label for="username">
                <span>Username</span>
                <input id="username" type="text" name="username" placeholder="Username" />
            </label>
            <label for="password">
                <span>Password</span>
                <input id="password" type="password" name="password" placeholder="Password" />
            </label>
            <label>
                <input id="terms" type="checkbox" required value="checked" name="terms" />
                <span>I accept the <a href="">terms & conditions</a></span>
            </label>
            <button type="submit">Create account</button>
        <% } %>
    </form>
    <%- include("partials/footer.ejs") %>